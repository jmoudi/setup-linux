

function createNewNodeProject(){

};

function create(){
    declare -A args={
        "--verbose"
        ""
        "--backup-dir=DIR"
        ""
};

function runRsync(){
    # as array
    local rsyncArgs="$1";
    rsync "
};
function update(){
    declare -A args=(
        "--verbose"
        "--update"
        "--backup-dir=DIR"
        ""
    );

};

    declare -A args={
        "--verbose"
        ""
        "--backup-dir=DIR"
        ""


alias